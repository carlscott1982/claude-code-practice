{
  "name": "spendlog-native",
  "version": "1.0.0",
  "description": "Capacitor wrapper that packages the Spend Log web app as an Android APK.",
  "scripts": {
    "sync": "node scripts/sync-web.mjs && npx cap sync android",
    "apk": "npm run sync && cd android && ./gradlew assembleDebug"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  },
  "private": true
}
