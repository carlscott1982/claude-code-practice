# Python
__pycache__/
*.py[cod]
.pytest_cache/

# Node — these patterns are anchored to the repo root on purpose.
# Unanchored, they would also swallow native/package.json, which the
# Android build genuinely needs.
node_modules/
/package.json
/package-lock.json
